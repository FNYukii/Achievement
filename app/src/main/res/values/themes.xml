<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BottomNav02" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/navy</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/gold</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <!-- Status Bar -->

        <!-- App Bar -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/navy</item>

        <!-- App background -->
        <item name="android:windowBackground">@color/navy</item>

    </style>


    <!-- EditText -->
    <style name="CustomEditText" parent="Theme.AppCompat.DayNight">
        <item name="colorControlActivated">@color/white</item>
        <item name="android:background">@color/transparent</item>
    </style>


    <!-- ColorDialog -->
    <style name="CustomDialog" parent="Theme.AppCompat.DayNight.Dialog">

        <!-- background -->
        <item name="android:background">@color/navy</item>

        <!-- title text -->
        <item name="android:textColor">@color/white</item>

        <!-- single choice items text -->
        <item name="android:textColorAlertDialogListItem">@color/gray</item>

        <!-- single choice items radio button -->
        <item name="colorControlNormal">@color/gray</item>
        <item name="colorControlActivated">@color/white</item>
    </style>


    <!-- Search View -->
    <style name="CustomSearchView" parent="Theme.AppCompat">
        <item name="android:background" tools:ignore="NewApi">@color/transparent</item>
    </style>


</resources>